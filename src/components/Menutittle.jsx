import React from 'react'

const Menutittle = ({text,className}) => {
  return (
    <h3 className={`${className} text-liHover text-[16px] font-bold font-dm`}>{text}</h3>
  )
}

export default Menutittle